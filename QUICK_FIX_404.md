# 404エラー クイック修正

## 即座に試すべき解決方法

### ステップ1: 開発サーバーを停止
現在実行中の `npm run dev` を停止してください（Ctrl+C または Cmd+C）

### ステップ2: キャッシュをクリア
```bash
npm run clean
```

### ステップ3: 開発サーバーを再起動
```bash
npm run dev
```

### ステップ4: ブラウザで確認
1. ブラウザで `http://localhost:3000` を開く
2. 開発者ツール（F12）を開く
3. ネットワークタブで404エラーが解消されているか確認

## それでも解決しない場合

### 方法A: 一括コマンドで実行
```bash
npm run dev:clean
```

### 方法B: スクリプトを使用
```bash
./scripts/fix-404-errors.sh
npm run dev
```

### 方法C: 完全リセット
```bash
# 1. 開発サーバーを停止
# 2. キャッシュをクリア
rm -rf .next node_modules/.cache

# 3. 開発サーバーを再起動
npm run dev
```

## 確認ポイント

✅ 開発サーバーが正常に起動している（ターミナルに "Ready" と表示される）
✅ ブラウザでページが表示される
✅ コンソールに404エラーが表示されない
✅ CSSが正しく適用されている（背景色などが表示される）

## よくある原因

1. **開発サーバーが正常に起動していない**
   - 解決: 開発サーバーを再起動

2. **ビルドキャッシュが破損している**
   - 解決: `.next`ディレクトリを削除

3. **ポートが既に使用されている**
   - 解決: 別のポートを使用するか、既存のプロセスを終了

4. **ファイルパスの問題**
   - 解決: `app/layout.tsx`で`import './globals.css'`が正しく記述されているか確認

