# 404エラー修正の実行手順

## 手順1: 開発サーバーを停止（手動で実行が必要）

### 方法A: ターミナルで実行中のサーバーを停止
1. `npm run dev` を実行しているターミナルウィンドウを開く
2. `Ctrl + C`（Mac/Linux）または `Cmd + C`（Mac）を押してサーバーを停止
3. ターミナルに "Process terminated" または "Server stopped" と表示されることを確認

### 方法B: プロセスを強制終了（方法Aで停止できない場合）
```bash
# ポート3000で実行中のプロセスを確認
lsof -ti:3000

# プロセスを強制終了（上記で表示されたPIDを使用）
kill -9 $(lsof -ti:3000)
```

## 手順2: キャッシュをクリアして開発サーバーを再起動

以下のコマンドを実行してください：

```bash
cd /Users/msys/project-app
npm run dev:clean
```

このコマンドは以下を実行します：
1. `.next`ディレクトリを削除（キャッシュクリア）
2. 開発サーバーを起動

## 手順3: ブラウザで動作を確認

1. ブラウザで `http://localhost:3000` を開く
2. 開発者ツール（F12）を開く
3. 以下を確認：
   - **コンソールタブ**: 404エラーが表示されていないか確認
   - **ネットワークタブ**: 
     - `_next/static/css/app/layout.css` が200ステータスで読み込まれているか確認
     - `_next/static/chunks/main-app.js` が200ステータスで読み込まれているか確認
   - **Elementsタブ**: 
     - CSSが正しく適用されているか確認（背景色など）
     - `<html>`タグに`dark`クラスが適用されているか確認（ダークモードの場合）

## 確認ポイント

✅ 開発サーバーが正常に起動している（ターミナルに "Ready" と表示される）
✅ ブラウザでページが表示される
✅ コンソールに404エラーが表示されない
✅ CSSが正しく適用されている（背景色などが表示される）
✅ クリック動作が正常に機能する

## トラブルシューティング

### ポートが既に使用されている場合

```bash
# ポート3000を確認
lsof -ti:3000

# プロセスを終了
kill -9 $(lsof -ti:3000)

# 再度起動
npm run dev:clean
```

### それでも解決しない場合

```bash
# 完全リセット
rm -rf .next node_modules/.cache
npm run dev
```

