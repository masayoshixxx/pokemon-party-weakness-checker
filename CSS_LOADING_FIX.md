# CSS読み込み問題の原因と再発防止策

## 問題の原因

### 1. **Next.js App Routerの制約**
- Next.js App Routerでは、`<head>`タグを直接使用することができません
- Next.jsが自動的に`<head>`を管理するため、カスタムスクリプトを追加する場合は特別な方法が必要です

### 2. **テーマスクリプトの実行タイミング**
- リロード時に、テーマクラス（`dark`）が適用される前にCSSが読み込まれる
- その結果、ダークモードのスタイルが適用されない

### 3. **ハイドレーションエラー**
- サーバーサイドとクライアントサイドでHTMLが一致しない場合、CSSが正しく適用されない可能性がある

## 解決策

### 修正内容
1. `layout.tsx`の`<body>`タグの最初にインラインスクリプトを配置
2. スクリプトを即座に実行（IIFE）して、CSS読み込み前にテーマクラスを適用
3. `suppressHydrationWarning`を追加してハイドレーションエラーを防止
4. `app/theme-script.tsx`を削除（不要になったため）

### コードの変更点
- `app/theme-script.tsx`を削除（不要になったため）
- `app/layout.tsx`の`<body>`タグの最初にインラインスクリプトを配置
- スクリプトを即座に実行（IIFE）して、CSS読み込み前にテーマクラスを適用
- スクリプトを`dangerouslySetInnerHTML`で直接記述

## 再発防止策

### 1. **Next.js App Routerのベストプラクティスに従う**
- `<head>`タグを直接使用しない（Next.js App Routerでは使用できない）
- CSS読み込み前に実行する必要があるスクリプトは、`<body>`タグの最初にインラインスクリプトとして配置する
- インラインスクリプトは即座に実行（IIFE）する形式で記述する

### 2. **テーマ管理の原則**
- テーマクラスは、CSS読み込み前に適用する必要がある
- インラインスクリプトを使用して、ページ読み込み時に即座に実行する

### 3. **テストの実施**
- リロード後にCSSが正しく適用されることを確認
- ダークモードとライトモードの両方でテスト
- ブラウザのキャッシュをクリアしてテスト

### 4. **チェックリスト**
改修時に以下を確認：
- [ ] `layout.tsx`のテーマスクリプトが正しく配置されているか
- [ ] `suppressHydrationWarning`が`<html>`タグに設定されているか
- [ ] CSSファイルが正しくインポートされているか（`import './globals.css'`）
- [ ] リロード後にCSSが適用されるか
- [ ] ダークモードとライトモードの両方で動作するか

### 5. **トラブルシューティング**
CSSが効かない場合の確認項目：
1. ブラウザの開発者ツール（F12）で、CSSファイルが読み込まれているか確認
2. `<html>`タグに`dark`クラスが適用されているか確認
3. コンソールにエラーが表示されていないか確認
4. ブラウザのキャッシュをクリア（Ctrl+Shift+R または Cmd+Shift+R）

## 今後の改善案

1. **CSS Modulesの使用を検討**
   - より確実にCSSを読み込む方法として、CSS Modulesの使用を検討

2. **テーマ管理ライブラリの導入**
   - `next-themes`などのライブラリを使用することで、より確実にテーマを管理できる

3. **テストの自動化**
   - E2Eテスト（Playwrightなど）で、CSS読み込みを自動テスト

