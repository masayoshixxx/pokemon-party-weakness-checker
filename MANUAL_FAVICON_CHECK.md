# Faviconの手動確認手順

## Playwrightのブラウザインストール

Playwrightのブラウザがインストールされていない場合は、以下のコマンドを実行してください：

```bash
npx playwright install chromium
```

## 手動での確認方法

### ステップ1: ブラウザでページを開く

1. ブラウザで `http://localhost:3000` を開く
2. 開発者ツール（F12）を開く

### ステップ2: ネットワークタブで確認

1. 開発者ツールの「ネットワーク」タブを開く
2. ページをリロード（Ctrl+R または Cmd+R）
3. 以下を確認：
   - `favicon.ico`のリクエストが404エラーになっていないか
   - `icon.svg`が200ステータスで読み込まれているか

### ステップ3: Elementsタブで確認

1. 開発者ツールの「Elements」タブを開く
2. `<head>`タグ内を確認
3. 以下が存在することを確認：
   ```html
   <link rel="icon" href="/icon.svg" type="image/svg+xml">
   ```

### ステップ4: ブラウザのタブで確認

1. ブラウザのタブにアイコン（⚡マーク）が表示されているか確認
2. アイコンが表示されていれば、faviconは正しく設定されています

## 期待される結果

✅ `favicon.ico`の404エラーが発生しない（または、`icon.svg`が使用される）
✅ ブラウザのタブにアイコンが表示される
✅ `/icon.svg`が200ステータスで読み込まれる
✅ `<head>`タグ内に`<link rel="icon">`が存在する

## トラブルシューティング

### faviconが表示されない場合

1. ブラウザのキャッシュをクリア（Ctrl+Shift+R または Cmd+Shift+R）
2. `app/icon.svg`が存在するか確認
3. `app/layout.tsx`の`metadata`に`icons`フィールドが設定されているか確認

### 404エラーが発生する場合

1. 開発サーバーを再起動
2. `.next`ディレクトリを削除して再ビルド
3. `app/icon.svg`が正しく配置されているか確認

