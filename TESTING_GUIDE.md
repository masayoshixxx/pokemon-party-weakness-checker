# 動作確認テストガイド

## テストの実行方法

### 1. 基本的な機能テスト

```bash
# Playwrightのブラウザをインストール（初回のみ）
npx playwright install

# 基本的な機能テストを実行
BASE_URL=http://localhost:3001 npm run test -- tests/basic-functionality.spec.ts
```

### 2. コンソールエラーの確認

```bash
# コンソールエラーを確認
BASE_URL=http://localhost:3001 npm run test:errors
```

### 3. すべてのテストを実行

```bash
# すべてのテストを実行
BASE_URL=http://localhost:3001 npm test
```

## 手動での動作確認

### CSSの確認

1. ブラウザで `http://localhost:3001` を開く
2. 開発者ツール（F12）を開く
3. コンソールタブでエラーがないか確認
4. Elementsタブで`<html>`タグに`dark`クラスが適用されているか確認（ダークモードの場合）
5. 背景色が正しく表示されているか確認

### クリック動作の確認

1. **テーマトグルボタン**
   - 右上のテーマトグルボタンをクリック
   - メニューが表示されることを確認
   - 各テーマオプションをクリックして動作を確認

2. **追加ボタン**
   - 「＋ 追加」ボタンをクリック
   - 新しいメンバーが追加されることを確認

3. **提案ボタン**
   - 「＋ 提案」ボタンをクリック
   - 新しいメンバーが追加されることを確認（提案が成功した場合）

4. **タイプ選択**
   - 各メンバーのタイプ1、タイプ2を選択
   - 選択が反映されることを確認

5. **削除ボタン**
   - メンバーが2匹以上の場合、削除ボタンをクリック
   - メンバーが削除されることを確認

6. **弱点計算についてボタン**
   - 「弱点計算について」ボタンをクリック
   - 説明が表示/非表示されることを確認

## トラブルシューティング

### CSSが効かない場合

1. ブラウザのキャッシュをクリア（Ctrl+Shift+R または Cmd+Shift+R）
2. 開発者ツールでCSSファイルが読み込まれているか確認
3. `<html>`タグに`dark`クラスが適用されているか確認
4. コンソールにエラーが表示されていないか確認

### クリックが動作しない場合

1. 開発者ツールのコンソールでエラーを確認
2. イベントリスナーが正しく設定されているか確認
3. ボタンが正しく表示されているか確認（CSSで非表示になっていないか）
4. JavaScriptが有効になっているか確認

## 自動テストの追加

新しい機能を追加した場合は、`tests/basic-functionality.spec.ts`にテストを追加してください。

